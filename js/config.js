// If API is running on different port:
window.AUTH_API_BASE = 'http://localhost:4000';
// If API is running on same localhost and port:
// window.AUTH_API_BASE = window.AUTH_API_BASE !== undefined ? window.AUTH_API_BASE : '';